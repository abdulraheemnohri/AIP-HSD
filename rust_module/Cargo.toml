[package]
name = "aiphsd-risk-scorer"
version = "0.1.0"
edition = "2021"

[lib]
name = "risk_scorer"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
